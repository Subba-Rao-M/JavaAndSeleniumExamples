package oopsConcepts.encpasulation;

public class EncapsulationDemo {
	
	/**
	 * Encapsulation is wrapping of data and methods in single unit(Class)
	 * You cannot directly access the variables
	 * Should access the class variables only through methods
	 * Security is achieved by  calling the methods of class which uses class variables
	 * All variables should be marked as private, so class methods can access it and operate on it
	 * Objects cannot access these variables
	 * Each variable will have 2 methods one for setting value and other for getting values called getter and setter method
	 */

	public static void main(String[] args) {
		

	}

}
